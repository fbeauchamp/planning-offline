/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';
    console.log('route loaded')

    var MainRouteRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return MainRouteRouter;
});
